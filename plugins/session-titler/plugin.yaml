name: session-titler
version: 1.1.0
description: "Auto-generate session titles on /quit and via /retitle command. Runs via pre_command hook — zero overhead on normal turns."
author: "Hauzer S. Lee"
hooks:
  - pre_command
